use crate::gfx::types::{Vertex};
use crate::gfx::renderer::Renderer;
use crate::app::App;

pub fn render_toasts(_renderer: &mut Renderer, _app: &App, _vertices: &mut Vec<Vertex>) {
    // Toast rendering is currently handled in app.rs
    // TODO: Move toast rendering logic here for better encapsulation
}

