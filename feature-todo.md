system prompt management:
create system prompts and save them as instructions, invoke them by @'ing them in the chat bar.

diskANN integration

wikipedia integration (way down the line, need storage device)

pdf rendering

finish notepad polish

bouncy star graph

on-device DB (later): consider a small local DB (e.g. SQLite) for conversation/chat storage—atomic writes, one source of truth, easier querying.